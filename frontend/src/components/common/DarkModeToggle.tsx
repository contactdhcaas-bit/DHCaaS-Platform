import React from "react";

export const DarkModeToggle: React.FC = () => {
  return (
    <button
      type="button"
      className="px-2 py-1 rounded border border-slate-200 dark:border-slate-700 text-sm"
      onClick={() => {}}
      aria-label="Toggle dark mode"
    >
      Theme
    </button>
  );
};
